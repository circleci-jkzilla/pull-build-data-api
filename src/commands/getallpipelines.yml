description: >
  This command calls get all pipelines
# What will this command do?
steps:
  - run:
      name: call get all pipelines api
      command: |
        <<include(scripts/getallpipelines.sh)>>
